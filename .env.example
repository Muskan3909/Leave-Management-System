# This project has two services, each with its own environment file:
#
#   backend/.env.example   -> copy to backend/.env
#   frontend/.env.example  -> copy to frontend/.env
#
# See README.md "Environment Variables" for what each value does.
